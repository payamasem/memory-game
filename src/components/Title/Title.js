import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Trump Memory Game!</h1>
        <p>Click on a Trump to earn points, but don't click on any Trump more than once!</p>
    </div>
);
export default Title;